-- Neovim config tuned for Go development.
-- Layout: config/ holds editor settings, plugins/ holds one file per concern.

require("config.options")
require("config.lazy")
require("config.keymaps")
require("config.autocmds")
